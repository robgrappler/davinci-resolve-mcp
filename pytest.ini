[pytest]
# Only tests/unit runs without a live DaVinci Resolve instance.
# Everything else matching test_* (tests/*.py, examples/, the root-level
# diagnostic scripts) requires Resolve running and is meant to be invoked
# manually, so keep pytest collection away from it.
testpaths = tests/unit
