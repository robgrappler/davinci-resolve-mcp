# DaVinci Resolve MCP Server Dependencies

# MCP Protocol SDK with CLI support (this is the official
# modelcontextprotocol/python-sdk package on PyPI)
mcp[cli]

# Git repository dependencies
git+https://github.com/modelcontextprotocol/python-sdk.git

# Dependencies for testing and automation scripts
requests>=2.28.0
psutil>=5.9.0
pytest>=7.0.0

# JSON-RPC Server for API
jsonrpcserver>=5.0.0
