# DaVinci Resolve MCP Server Dependencies

# MCP Protocol SDK with CLI support (this is the official
# modelcontextprotocol/python-sdk package on PyPI).
# Pinned below 2.0: the 2.x line removes mcp.server.fastmcp,
# which this server imports. Do not add a git+https line for the
# python-sdk repo — it resolves to unreleased 2.x dev builds.
mcp[cli]>=1.3,<2.0

# --- Dev/test-only dependencies below ---
# Used only by tests/ and scripts/batch_automation.py, not by the server itself.
requests>=2.28.0
psutil>=5.9.0
jsonrpcserver>=5.0.0
pytest>=7.0.0
